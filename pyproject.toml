[project]
name = "winconfigbkp"
version = "1.0.0"
description = "Windows 配置文件备份工具"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.8",
    "json5>=0.10",
    "watchdog>=6.0",
    "pyinstaller>=6.12",
]
